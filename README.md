## Setup
1. Get an API key from [OpenWeatherMap](https://openweathermap.org/api)
2. Create a `secret.properties` file in the project root
3. Add: `WEATHER_API_KEY=your_actual_api_key_here`
4. Build and run the app
